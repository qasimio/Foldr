[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "foldr"
version = "0.1.0"
description = "Simple file organizer by extension"
authors = [
  { name = "Muhammad Qasim", email = "amkassim444@gmail.com" }
]
requires-python = ">=3.9"

[project.scripts]
foldr = "file_organizer.cli:main"